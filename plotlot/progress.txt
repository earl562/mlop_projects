# Ralph Progress Log

Started: 2026-06-26
Branch: dev (monolithic Ralph — single branch, one task per iteration)
Plan: IMPLEMENTATION_PLAN.md
Skill: ~/.pi/agent/skills/plotlot-ralph-loop/SKILL.md (v2.1.0, verified Ralph + PageAI/snarktank/ghuntley enhancements)

---

## Codebase Patterns

Curated, reusable learnings consolidated from the log below (snarktank pattern).
General + reusable only; story-specific details stay in the dated log entries.

- **Never `git stash`** (AGENTS.md + Slice 0.2). Use a preservation commit or `git checkout -- <file>`.
- **After `git commit --amend`**, re-capture the hash: `FINAL_HASH=$(git log -1 --format=%H)`. Pre-amend hashes are stale.
- **ArcGIS layer URLs are the layer resource** (`.../FeatureServer/0`), not the query endpoint. `_query_nearby_sales` must append `/query`. Registered `SalesSource.layer_url` values do NOT end in `/query`.
- **ArcGIS metadata listings diverge from live schemas.** Before trusting registered field names, reconcile against the live `/<layer>?f=json`. Slice 1.2 shipped `PARCELNO`/`LOTSQFT` that didn't exist on the live Palm Beach layer.
- **`_find_field` uses substring fallback** (added Slice 1.2). DB-qualified (`SQLGIS02.dbo.BCPA_SALES.SALE_AMOUNT`) and suffixed (`PRICE_1`, `DOS_1`) names match via contains, not just exact.
- **`_PRICE_FIELDS` includes `SALE_AMOUNT`**; **`_DATE_FIELDS` includes `DOS`, `DATEOFSALE`** (added Slice 1.2 for Broward + Miami-Dade).
- **`safe_float` strips `$` and commas**; `_parse_sale_date` handles epoch ms. Broward's `"$1,850,000"` string and epoch-ms dates parse correctly.
- **Live integration tests** live in `tests/integration/` and are gated by `PLOTLOT_LIVE_TESTS=1`. The deterministic gate (`make verify-local`) skips them. Run explicitly before any "feature works" claim.
- **CLAIM-WITHOUT-EVIDENCE GUARD:** unit tests passing ≠ feature works. Before claiming a feature works for a real input, run it live + assert sane output + write an integration test pinning it. (Skill v2.1 step 6.)
- **`domain/` layer convention:** frozen dataclasses with `slots=True` + `__post_init__` invariants (see `dimensional_standard.py`, `claims.py`). A frozen dataclass CANNOT be mutated even via `object.__new__`, so predicate-form invariants (e.g. `source_boundary_ok()`) exist to validate *deserialized* rows that bypassed construction — test them with a lightweight stub dataclass carrying matching attrs, not by mutating a Claim.
- **Source boundary (Slice 2.1):** `zoning.*` / `parcel.*` claims must be `origin=local_authority` (the Rehab Valuator corpus is NOT a zoning oracle). `cost.*` / `cap_rate` / `financing.*` / `rent.*` must never be `kind=verified_fact`. Both raise `SourceBoundaryViolation` at construction. Downstream `steps.py`/`guardrails.py` build rules *over* `Claim`, not new types. The canonical namespace sets are the PUBLIC module constants `LOCAL_AUTHORITY_NAMESPACES` / `ASSUMPTION_NAMESPACES` in `claims.py` (import them — never re-declare, to avoid drift).
- **Guardrails (Slice 2.3) vs constructor invariants:** rules 1–3 of `domain/guardrails.py` overlap the `Claim.__post_init__` invariants, but the guardrails run over a *ClaimLog* (loaded from storage / aggregated across runs / tool-fed) whose rows BYPASS construction. So the guardrails are the SET-level re-validation (report-validator foundation, spec acceptance #7). Test rule-violating log rows with a STUB dataclass (matching attrs + `namespace` property, no `__post_init__`) — a frozen `Claim` cannot be constructed in a violating state. A contradiction is an ESCALATION (`requires_human_review=True`), not an integrity failure.
- **Type relocation across layers (Slice 2.4):** when moving shared types to a lower layer, prefer re-export-by-identity (`from lower import X`) in the old location over mass-editing N consumer files. Identity is cheaply testable (`obj is obj`) and the shim lets new code migrate incrementally. Move a type WITH its dependency closure (e.g. `EvidenceItem` pulls `EvidenceCitation`/`SourceType`), else you force an up-import cycle or a divergent re-declaration. To assert "module X doesn't import from module Y" in a test, walk the parsed AST `Import`/`ImportFrom` nodes — substring-matching the source catches docstring mentions (false positives).
- **Mocking SQLAlchemy's `pg_insert` in unit tests (Slice 3.1):** the builder chain is `pg_insert(Model) -> .values(rows) -> .on_conflict_do_update(constraint=..., set_=...) -> stmt`. Use `MagicMock` for the stmt chain (it's sync), `AsyncMock` only for the session. `mock_insert.call_args[0][0]` is the MODEL; the rows are at `mock_stmt.values.call_args[0][0]`; the conflict call is at `mock_stmt.values.return_value.on_conflict_do_update.call_args` — three `.return_value` levels deep.
- **Structural vs embedding unit (Slice 3.1):** `OrdinanceChunk` is the embedding/retrieval unit; `OrdinanceSection` is the structural unit (one row per municode node, keyed (municipality, node_id)) carrying path/section_type/cross_refs/referenced_by. A section's chunks all share its path/cross_refs/section_type. The reverse-index `referenced_by` is owned by the Slice 3.5 backfill — ingestion upserts must OMIT it from `set_` so a refresh never clobbers the backfill-built reverse index. Table is provisioned by `init_db`→`Base.metadata.create_all`; migrations are belt-and-suspenders (idempotent) for the operator-managed alembic path.

---

## Iteration history (newest at bottom)

### Pre-Ralph (manual TDD, not true Ralph — kept for continuity)
- 2026-06-25 Slice 0.1 — branch reckoning. Froze `feature/deal-analysis-pipeline` (59 commits, 331 files preserved). Fresh `work/harness-build` from `origin/dev`. Zillow/stealth liability absent from working tree. Ralph skill installed as pi builtin. <promise>COMPLETE</promise>
- 2026-06-25 Slice 0.2 — MLflow gate fix. conftest self-sets `MLFLOW_ALLOW_FILE_STORE`; verify script exports on 3 pytest paths; bundled 2 pre-existing tsc fixes. Merged `3d0a126`. <promise>COMPLETE</promise>
  - LESSON: `git stash` silently reverted a conftest edit; caught only by re-running contract test without env var. AGENTS.md forbids stash for a reason. Never stash.
  - LESSON: after `git commit --amend`, re-read the hash before cherry-pick. Cherry-picked pre-amend hash → wrong commit landed. Re-capture with `git log -1 --format=%H`.
- 2026-06-25 Slice 0.3 — jsdom localStorage polyfill in `tests/ui/setup.ts`. Gate fully green on `dev`. Merged `3d79f69`. <promise>COMPLETE</promise>
- 2026-06-25 Slice 1.1 — dimensional extraction spike PASSED. `src/plotlot/domain/dimensional_standard.py` + `extract_dimensional_standards()`. Verified-fact path confirmed real. Merged `4ccbf74`. <promise>COMPLETE</promise>
  - FINDING: `dev` already has `_normalize_zone_tables()` in `ingestion/adapters/codifier.py` (commit `bc9b31c`) — it normalizes markdown tables for the LLM but does NOT extract typed rows. Slice 1.1 built on top of it; didn't duplicate. Future dimensional work (3.2) should generalize the extractor, not re-normalize.
  - FINDING: `dev` has substantial on-thesis work the feature branch lacked: upzoning engine (`c279e5e`), RentCast comps (`394be70`), codifier table reconstruction (`bc9b31c`), anti-hallucination gating (`79883d9`). The feature branch was not the only place work happened.

## Signs (operator learnings — Ralph reads these each iteration)

- Never `git stash` (AGENTS.md + Slice 0.2 lesson). Use preservation commit or `git checkout -- <file>`.
- After any `git commit --amend`, re-capture hash: `FINAL_HASH=$(git log -1 --format=%H)`.
- Cherry-pick (not `--no-ff` merge) is the workflow to `dev`. One slice per commit.
- `make verify-local` is the backpressure gate. Non-negotiable before push.
- Explicit `git add <paths>` only — never `-A` (AGENTS.md).
- Earl Perry commits, no Co-Authored-By trailers (AGENTS.md).
- Before implementing: search codebase ("don't assume not implemented") — ghuntley's rule. `dev` has more than the feature branch did.
- No placeholder implementations. Full implementations or BLOCKED.
- Every iteration ends with a `<promise>` tag. No silent endings.

## Open decisions (human must resolve)

- **Comps scraper (Slice 1.2):** operator wants a scraper. Skill v2 + plan encode the boundary rule: scraped comps = `kind=assumption, origin=unknown`, amber, confidence ≤ 0.5, gated behind Slice 9.1 governance. DO NOT silently violate this — if scraped comps flow into `value.arv` as `verified_fact`, the boundary Slice 1.1 built is broken at the moment of creation.

### 2026-06-26 — Slice 1.2 re-scope decision

DECIDE resolved. Operator clarified:
- RentCast is NOT free (50 req/mo free tier isn't production-viable) → drop RentCast dependency.
- ArcGIS county sales data is the comps path forward. Legal, evidenced, no CAPTCHA.
- Target: comps within 0-3 miles of subject.

This means Slice 1.2 is NOT a Zillow/stealth scraper. It's: verify/strengthen the
existing ArcGIS comps path in pipeline/comps.py, drop RentCast as a dependency,
ensure 0-3 mile radius coverage for South FL counties.

Boundary rule still holds: ArcGIS county sales = origin=local_authority (legal,
evidenced, sourced). This is the verified-fact path for comps, not an assumption.

<promise>DECIDE:resolved</promise> — proceeding to investigation.

### 2026-06-26 — Slice 1.2 (comps sources)
- DECIDE resolved: RentCast not free → drop. ArcGIS county sales is the path.
- Investigation finding (ghuntley rule: don't assume not implemented): comps.py
  already had find_comparables(radius_miles=3.0) + _query_nearby_sales + Hub
  discovery. The gap was the curated registry (comps_sources.py) was EMPTY.
- Verified live (rule #11): probed ArcGIS endpoints. Found:
  - Miami-Dade PaGISView FS/0: PRICE_1, DATEOFSALE_UTC (sale on parcel)
  - Broward BCPA MapServer/18 (2025 Sales): SALE_AMOUNT, SALE_DATE (year-bucketed)
  - Palm Beach Parcels FS/0: PRICE, SALE_DATE (sale on parcel)
- Implemented: registered 3 curated SalesSource entries with verified fields.
- Boundary intact: ArcGIS county sales = origin=local_authority (legal, evidenced).
  No scraper built. No CAPTCHA. No RentCast dependency for South FL.
- Merged ed34ec0. <promise>COMPLETE</promise>
- FINDING for future: Broward's sales are year-bucketed (layer 17=2026, 18=2025, 19=2024).
  A 12-month recency window may need to query multiple sibling layers for full coverage.
  Not solved in 1.2 (single-layer registration); flag for a follow-up if recency gaps appear.

### 2026-06-26 — Slice 1.2 verification gap (operator-caught)

OPERATOR CAUGHT A CLAIM-WITHOUT-EVIDENCE BUG. I shipped "comps path works for
South FL" in the Slice 1.2 commit message + summary, having verified only that
the ArcGIS endpoints exist with the right field names. I did NOT verify that
comps actually come back, are sane, or that find_comparables produces them.

Investigation after the catch found TWO real defects the registry test missed:
1. Broward's SALE_AMOUNT is a string "$1,850,000" — safe_float handles it, OK.
2. Broward's field names are DB-qualified (SQLGIS02.dbo.BCPA_SALES.SALE_AMOUNT);
   Miami-Dade's are PRICE_1 / DOS_1. _find_field did EXACT matching against
   candidate sets {SALE_AMT, PRICE} / {SALE_DATE} → 2 of 3 counties returned
   None for price_field → find_comparables silently returns 0 comps.
   Only Palm Beach (PRICE / SALE_DATE exact match) actually worked.

## NEW SIGN (permanent — Ralph reads this every iteration):

**CLAIM-WITHOUT-EVIDENCE GUARD.** A unit test passing is NOT evidence the
feature works. "Registered" ≠ "returns data" ≠ "returns accurate data" ≠
"the consumer (find_comparables) produces correct comps." Before any commit
message or summary claims a feature works for a real input, the iteration MUST:
  (a) run the feature against a REAL input (live query, real address), AND
  (b) assert the output is non-empty AND sane (real prices, real dates, in range), AND
  (c) write an integration test that pins (a)+(b) so it can't silently regress.
Commit messages may only claim what the tests + live verification prove.
"No analysis claim without evidence" (test-spec invariant) applies to me too.

<promise>DECIDE:resolved — proceeding to fix field-mapping + end-to-end verify</promise>

### 2026-06-26 — Slice 1.2 fix (claim-without-evidence gap closed)
- Operator caught: shipped "comps work for South FL" with only registry tests,
  no live proof. Live verification found THREE real defects the unit tests missed:
  1. _find_field exact-match only → Miami-Dade PRICE_1 + Broward DB-qualified
     names unresolved → 0 comps. Fix: substring fallback + new candidates.
  2. Palm Beach registry fields didn't exist on live layer (metadata listing
     diverged from actual schema). Fix: reconciled against live /?f=json.
  3. _query_nearby_sales GET'd the layer resource (.../FeatureServer/0) not
     /query → ArcGIS returned service metadata (0 features). Fix: append /query.
- EVIDENCE: find_comparables(Palm Beach, Wellington, 3mi, 60mo) → 3 real comps
  ($2.175M, $1.605M, $2.385M, all May 2026, 1.3-2.8mi). Pinned by live
  integration test tests/integration/test_comps_south_fl_live.py.
- Merged 227b815. <promise>COMPLETE</promise>
- LESSON: live end-to-end verification caught what 4 unit tests + a registry
  test missed. The CLAIM-WITHOUT-EVIDENCE GUARD (sign above) is now proven
  load-bearing, not theoretical. Apply it every slice.

### 2026-06-26 — Ralph skill v2.2: real outer loop + global skill

Skill is now a GLOBAL pi skill at ~/.pi/agent/skills/plotlot-ralph-loop/ (not in
the repo). Ships with: SKILL.md (v2.2), ralph.sh (the actual bash outer loop),
PROMPT_build.md, PROMPT_plan.md.

This fixes the architectural honesty gap: I'd been emulating Ralph inside one pi
session (context accumulating across iterations). Real Ralph = bash outer loop
re-invoking the agent with fresh context each iteration (ghuntley canonical form).
ralph.sh does: preflight → archive-on-branch-change → feed PROMPT_*.md to pi
--print --no-session → detect <promise> tags → push → loop/exit (0/1/2/3).

Skill v2.2 also encodes the 3 mechanics I missed in v2.1:
- Subagent allocation (ghuntley): main = scheduler, ONE validator subagent.
- Acceptance-driven backpressure: planning derives required tests from acceptance
  criteria; build can't claim passes:true without them. This is the structural
  fix for the Slice 1.2 claim-without-evidence class.
- specs/*.md directory (ghuntley): per-JTBD requirement specs as plan source.

Repo: created specs/agentic-zoning-harness.md (first JTBD spec with acceptance
criteria + derived required tests). IMPLEMENTATION_PLAN.md tasks should now carry
their required tests; a future planning-mode iteration will backfill that.

<promise>COMPLETE</promise>

### 2026-06-26 — Slice 2.1: typed-claim spine (domain/claims.py)
- Added src/plotlot/domain/claims.py: `Claim` (frozen dataclass, slots),
  `ClaimKind` (verified_fact/assumption/hypothesis/calculation/contradiction),
  `ClaimOrigin` (local_authority/rehabvaluator_concept/user_provided/
  derived_calc/unknown), `SourceBoundaryViolation`, `source_boundary_ok()`.
- The constructor enforces the source boundary (Rehab Valuator corpus is
  authoritative for concepts, NOT local facts):
  1. zoning.* / parcel.* MUST originate from local_authority (corpus, user,
     derived_calc, unknown all rejected).
  2. cost.* / cap_rate / financing.* / rent.* must NEVER be verified_fact
     (market numbers are assumptions).
  3. kind=hypothesis requires next_verification_step (entitlement upside
     can't be presented as guaranteed).
  4. kind=contradiction requires metadata["contradicts"] (both sides named).
  5. origin=unknown confidence capped ≤ 0.5 (scraped comps = amber).
- Exported from domain/__init__.py. Transport-free: no imports from
  harness/api/tools/retrieval.
- EVIDENCE: 18 contract tests pass (tests/unit/test_claims.py). The two
  spec-derived boundary tests are parametrized over all non-local-authority
  origins and all assumption namespaces. `make verify-local` green.
- LEARNING (Codebase Patterns): domain layer uses frozen dataclasses with
  slots + __post_init__ invariants (see dimensional_standard.py). A frozen
  dataclass CANNOT be mutated even via object.__new__, so the predicate form
  `source_boundary_ok()` exists to validate deserialized rows that bypassed
  construction — tested via a lightweight stub dataclass with matching attrs.
- LESSON: the spec's required tests are the acceptance bar. Slice 2.1's
  required tests ("zoning.*+non-local-authority raises; cost.*+verified_fact
  raises") are exactly what I wrote. Good alignment with planning.
- Merged dev+commit. <promise>COMPLETE</promise>

### 2026-06-26 — Ralph skill verify-live (ralph.sh --once)
RAN THE REAL OUTER LOOP. `ralph.sh --once` from PROJECT_ROOT:
1. Preflight passed (AGENTS.md + IMPLEMENTATION_PLAN.md + progress.txt present).
2. Fed PROMPT_build.md to `pi --print --no-session` (fresh context).
3. Agent followed the skill: oriented, selected Slice 2.1 (highest-priority
   incomplete), implemented domain/claims.py with source-boundary invariant,
   ran gate, committed, pushed.
4. Agent emitted <promise>COMPLETE</promise>.
5. ralph.sh detected the tag via grep, exited 0.
Result: Slice 2.1 landed (commits 870231a + 102beaa), gate green, 18 contract
tests pass. The skill WORKS end-to-end with pi — not just theory.

VERIFY-LIVE of the boundary invariant itself:
- zoning.district + origin=rehabvaluator_concept → raises SourceBoundaryViolation ✅
- cost.hard_cost_psf + kind=verified_fact → raises SourceBoundaryViolation ✅
- zoning.district + origin=local_authority → valid (created without raising) ✅
Design note: the iteration enforced the invariant by raising on construction,
not via a stored `source_boundary_ok` boolean. Functionally equivalent + stronger.
This is a Pattern-worthy deviation from the spec's literal wording — specs say
WHAT to verify, implementation decides HOW (ghuntley acceptance-driven rule).

LESSON: ralph.sh's PROMPT_FILE path was initially wrong (looked in PROJECT_ROOT,
but prompt files live in the global skill dir). Fixed to resolve from $SCRIPT_DIR
with a project-root override fallback. Caught by verify-live, not theory.

<promise>COMPLETE</promise>

### 2026-06-26 — Slice 2.2: Kleyman step table + HTN decomposition
- Added src/plotlot/domain/steps.py: `KleymanStep` IntEnum (1..8),
  `StepRequirement` (namespace + min_kind), `StepDef` (produces + requires),
  the exhaustive step table, and the blocking rule. `step_blocked_reasons`
  returns the namespaces whose preconditions are unsatisfied — empty tuple
  means the step may activate. The load-bearing rule: step 5
  (RESIDUAL_LAND_VALUE) requires `zoning` at verified_fact (spec acceptance #3),
  enforced by code not prompt. Steps 2/4 also gate on verified zoning
  (value derives from density derives from district).
- Added src/plotlot/domain/methodology.py: HTN task/method defs as data —
  `HtnMethod` (named decomposition alternative), `MethodDispatch`, `dispatch()`,
  `select_method()`. Step 2 (as-built value) carries TWO methods specialized-
  first: `step2.income_approach` (≥5 units, NOI/cap) and `step2.comp_approach`
  (≤4 units, ARV × units). Residential vs commercial divergence (spec
  acceptance #4) is a typed method dispatch keyed on `product.unit_count`,
  not a prompt hope. Boundary: 5→income, 4→comp.
- Exported from domain/__init__.py.
- EVIDENCE: 15 new contract tests in tests/unit/test_steps.py pass. They pin:
  (a) step 5 blocks when zoning missing, blocks when zoning only assumption-
  grade, unblocks when verified; (b) requirement_satisfied honors the
  verification ladder and rejects off-ladder kinds (calculation/contradiction
  never satisfy a precondition on their own); (c) step 2 dispatch selects
  income_approach at ≥5, comp_approach at ≤4, with the 4/5 boundary exact;
  (d) dispatch is pure (same claims → same selection); (e) every step has a
  dispatchable method. Combined with Slice 2.1's 18 tests = 33 domain tests
  green. `make verify-local` fully green.
- LEARNING (Codebase Patterns): the source boundary in claims.py forbids
  constructing a `zoning.assumption` claim (zoning.* must be local_authority).
  So the "unverified zoning blocks step 5" test can't model the bad case as a
  zoning claim with kind=assumption — it models it as the ABSENCE of a
  verified zoning claim. The kind ladder (hypothesis→assumption→verified_fact)
  is the gate, and a verified_fact requirement is only satisfied by a claim
  at or above that rung. Keep this in mind for Slice 2.3 guardrails + the
  planner: "blocked" often means "required claim is missing", not "required
  claim exists but is wrong grade".
- VERIFY-LIVE: this is a gate-only slice (pure data + functions over Claim[],
  no API/scraper/address). The contract tests ARE the live evidence — they
  exercise the rule against real Claim objects and assert the blocking + the
  dispatch both fire. Gate-only slices are exempt from the real-input
  requirement per the Ralph skill (Phase 5).
- LESSON: splitting steps.py (may-this-step-activate?) from methodology.py
  (which-sub-task-runs?) keeps the two HTN concerns clean — Slice 5.1 planner
  will call step_blocked_reasons() to gate activation and dispatch() to pick
  the sub-task sequence. Don't merge them; the blocking predicate is the more
  fundamental invariant.
- Merged dev+commit. <promise>COMPLETE</promise>

### 2026-06-26 — Slice 2.3: claim-log guardrails (the report validator foundation)
- Added src/plotlot/domain/guardrails.py: the 5 Kleyman guardrails as pure
  functions over `Claim[]`. `GuardrailRule` enum + `GuardrailViolation`
  (frozen dataclass) + `evaluate_guardrails(claims) -> tuple[Violation,...]`
  + per-rule `check_*` functions + `is_material()` + `requires_human_review()`
  + `integrity_violations()`/`human_review_violations()` partitioners.
- The 5 rules:
  1. LOCAL_AUTHORITY_ORIGIN — zoning.*/parcel.* must be origin=local_authority.
  2. ASSUMPTION_NAMESPACE — cost.*/cap_rate/financing.*/rent.* must NOT be
     kind=verified_fact (market numbers are always assumptions).
  3. HYPOTHESIS_VERIFICATION — kind=hypothesis needs next_verification_step
     (entitlement upside never presented as guaranteed).
  4. MATERIAL_EVIDENCE — material claims (verified_fact OR recommendation/
     metrics/value namespace) must carry >=1 evidence_id.
  5. CONTRADICTION_REVIEW — kind=contradiction surfaces for human review
     (requires_human_review=True — escalation, NOT integrity failure).
- KEY DESIGN: rules 1-3 overlap with the Claim constructor's __post_init__
  invariants. The guardrails exist because a ClaimLog is loaded from storage /
  aggregated across agent runs / fed by tools that BYPASS construction — the
  per-claim constructor can't validate those rows. So guardrails.py is the
  SET-level re-validation (the report validator foundation, spec acceptance #7:
  "report fails on missing evidence_ids / blurred boundaries / hidden
  contradictions"). The contradiction rule is intentionally NOT an integrity
  failure — a consistent log can still need adjudication.
- DRIFT PREVENTION: promoted claims.py's `_LOCAL_AUTHORITY_NAMESPACES` /
  `_ASSUMPTION_NAMESPACES` to public `LOCAL_AUTHORITY_NAMESPACES` /
  `ASSUMPTION_NAMESPACES` and imported them in guardrails.py — single source
  of truth for the boundary (the Codebase Patterns warn about divergent
  registries; this avoids a second copy of the namespace sets). Exported from
  domain/__init__.py.
- MATERIAL definition: `is_material(c)` = kind==verified_fact OR namespace in
  {recommendation, metrics, value}. Pure assumptions (user cost, market cap
  rate) are EXEMPT — they're already amber-rendered + provenance-tracked via
  source_url/origin, not evidence rows. Tested explicitly.
- EVIDENCE: 38 contract tests in tests/unit/test_domain_guardrails.py pass.
  They pin: each rule fires on its bad case + does NOT over-fire on clean
  claims; all namespaces in LOCAL_AUTHORITY/ASSUMPTION sets are covered;
  is_material() classification; contradiction is escalation-not-failure;
  evaluate_guardrails is deterministic + returns rules in stable declaration
  order; constructible Claims pass rules 1-3 (no false positives). Combined
  with Slices 2.1+2.2 = 71 domain tests green. `make verify-local` green.
- LEARNING (Codebase Patterns): rules that overlap constructor invariants must
  be tested with a STUB dataclass (matching attrs + namespace property, no
  __post_init__), not by mutating a frozen Claim — same pattern as
  source_boundary_ok predicate tests. A frozen Claim CANNOT be constructed in
  a rule-violating state, so the guardrail's "log row that bypassed
  construction" case can only be modeled by a stub.
- VERIFY-LIVE: gate-only slice (pure functions over Claim[], no API/address).
  Contract tests ARE the live evidence — they exercise each rule against real
  Claim + stub Claim objects and assert the violation fires with the right
  rule + field_key + requires_human_review flag. Also ran an end-to-end import
  sanity scenario (clean log → no violations; +contradiction → 1 escalation).
- Merged dev+commit. <promise>TASK-DONE</promise>

### 2026-06-26 — Slice 2.4: move shared types to domain/types.py
- Created src/plotlot/domain/types.py as the canonical home for the 5
  transport-agnostic shared types named in the spec — ToolContract,
  ToolContext, PolicyDecision, EvidenceItem, ReportClaim — plus their
  supporting dependency types that had to move with them to avoid an
  up-import cycle: DEFAULT_ORDINANCE_LEGAL_CAVEAT, SourceType,
  EvidenceConfidence, ToolRiskClass, EvidenceCitation,
  EvidenceBackedReportSection. Moving a type without its deps would force
  domain/types.py to import back from land_use (cycle) or land_use to
  re-declare the dep (divergent-registry hazard).
- Reduced src/plotlot/land_use/models.py to a thin service module: it now
  RE-EXPORTS the moved types (`from plotlot.domain.types import ...`) so
  both `from plotlot.land_use import ToolContract` and
  `from plotlot.land_use.models import EvidenceCitation` keep working
  unchanged across 27 consumer files. The service-specific ordinance/layer
  query types (OrdinanceJurisdiction, OrdinanceSearchArgs,
  OrdinanceSearchResult, LayerType, LayerCandidate, PropertyLayerQuery)
  stay in land_use/models.py — they're land-use-service-specific and depend
  on the shared citation/source/confidence types now living in domain.
- Exported all moved types from domain/__init__.py.
- KEY INVARIANT: re-export is by identity (same class object), not
  re-definition. isinstance checks across the two namespaces stay consistent
  — this is the divergent-registry hazard the Codebase Patterns flag.
- EVIDENCE: 5 new contract tests in tests/unit/test_domain_types_move.py:
  (a) the moved types live canonically in domain.types + domain pkg ns;
  (b) land_use.models re-exports the SAME identity (assert `is`, not just
  attrs-equal) — guards against a future regression that re-derives the
  type in land_use; (c) the 11 moved names import from the land_use package;
  (d) service-specific types remain in land_use.models and are NOT in
  domain.types; (e) domain.types has zero land_use imports (no-cycle
  invariant, checked via AST over import statements not docstring prose).
  Existing test_land_use_tool_contracts.py still passes (8/8).
- VERIFY-LIVE: gate-only slice (pure type relocation, no API/scraper/
  address). The contract tests ARE the live evidence — they assert the
  move invariants against the real imported module objects. Also ran a
  live import sanity check confirming ToolContract etc. resolve to the
  same object via both namespaces and still validate inputs.
- LEARNING (Codebase Patterns): when relocating types across layers, prefer
  re-export-by-identity in the old location over mass-editing 27 consumer
  files. Identity is testable cheaply (`obj is obj`) and the re-export
  shim lets new code migrate to the canonical path incrementally without
  a big-bang rename. The AST-over-imports pattern is the robust way to
  assert "module X doesn't import from module Y" — substring-matching the
  source catches docstring mentions (false positives).
- Merged dev+commit. <promise>TASK-DONE</promise>

### 2026-06-26 — Slice 3.1: OrdinanceSection model + chunker path/cross_refs/section_type
- `OrdinanceSection` is the STRUCTURAL unit over `OrdinanceChunk` (the embedding
  unit). One row per munique node, keyed by (municipality, node_id). It carries
  the path breadcrumb, section_type, outbound cross_refs, and a reverse-index
  referenced_by (default [] — populated by the Slice 3.5 backfill, NOT by
  re-ingestion: the upsert deliberately omits referenced_by from its update set
  so a refresh never clobbers the backfill-built reverse index).
- Three chunker helpers added to `ingestion/chunker.py`:
  - `_extract_cross_refs(text)` — outbound section-number refs from
    `§`/`Sec.`/`Section` prefixes → bare `47-24.3`-style keys.
  - `_classify_section_type(heading, title, text)` — dimensional_table |
    schedule | definition | use_regulation | regulation (catch-all). Drives the
    Phase 8 AgenticRAG dimensional-table fast-path (no LLM).
  - `_build_section_path(section, sec_num, title)` — prefers an explicit
    `RawSection.path` (full ancestor chain) else synthesizes [parent_heading,
    sec_num]. Blanks dropped.
- `RawSection` got an optional `path: list[str] | None` field; `ChunkMetadata`
  got `path` (default []), `cross_refs` (default []), `section_type`
  (default "regulation"). All three are populated once per section in
  `chunk_sections`, so every chunk of a section shares them.
- Ingestion now upserts sections: `_store_ordinance_sections(session, chunks,
  config)` dedups by node_id, unions cross_refs defensively, upserts on the
  `uq_section_natural_key` constraint. Wired into `ingest_municipality` after the
  chunk store (logs `ingest.sections_indexed`). San Diego's PDF path
  (`ingest_san_diego`) is NOT wired this slice — it builds TextChunks directly
  without RawSections; Slice 3.5 backfill covers PDF-only sources via synthetic
  node_ids. This slice is the Municode path only.
- Migration `009_ordinance_sections.py` (idempotent CREATE TABLE IF NOT EXISTS +
  DO $$ constraint + CREATE INDEX IF NOT EXISTS). NOTE: runtime provisions via
  `init_db` → `Base.metadata.create_all`, so the table is auto-created on first
  connect regardless of migration apply state; migration exists for the
  operator-managed alembic path. Also documented a PRE-EXISTING alembic anomaly
  (duplicate revision ids "007" x2, "008" x2; `alembic heads` reports two heads
  both named "008") — NOT introduced by this slice, separate operator fix.
- EVIDENCE: 12 new tests (chunker cross-refs/classifier/path-builder +
  share-metadata; model structural columns + natural-key + ARRAY types +
  defaults; ingest upsert dedup + conflict target + referenced_by-excluded +
  skip-no-node-id + distinct-rows). Two pre-existing ingest tests updated:
  execute/commit now called twice (chunks + sections). Gate green
  (`make verify-local`).
- VERIFY-LIVE: chunker is live-behavior (real ordinance-shaped HTML input).
  Ran it end-to-end on a Broward §47-5.60 dimensional table + a §47-2
  definitions section: section_type classified correctly
  (dimensional_table/definition), path = [Chapter, Sec], cross_refs captured
  `47-24.3` + `47-5.601`. ORM model declares all structural columns + the
  `uq_section_natural_key` constraint. The migration itself is gate-only
  (table auto-created via create_all; no DB in the sandbox) — its SQL is
  idempotent and unit-asserted via the model's column/constraint declarations.
- LEARNING (Codebase Patterns): when patching `pg_insert` (SQLAlchemy's
  postgres dialect insert factory) in a unit test, the call chain is
  `pg_insert(Model) -> .values(rows) -> .on_conflict_do_update(...) -> stmt`.
  `mock_insert.call_args[0][0]` is the MODEL, not the rows; the rows are on
  `mock_stmt.values.call_args[0][0]`, and the conflict call is on
  `mock_stmt.values.return_value.on_conflict_do_update.call_args` — three
  levels of `.return_value` deep, easy to mis-attribute. Use `MagicMock`
  (not `AsyncMock`) for the stmt chain: `.execute(stmt)` is awaited, but the
  builder chain is sync.
- Merged dev+commit. <promise>TASK-DONE</promise>

### 2026-06-26 — Planning iteration: prd.json + Carson semantics

Converted IMPLEMENTATION_PLAN.md to prd.json (Carson format) with:
- 15 stories, each with explicit acceptanceCriteria (behavioral + connection).
- 2 backfill wiring slices (WIRE-1.1b, WIRE-2.1b) prioritized FIRST — these
  connect the island modules (calculate_max_units ← DistrictDimensionalStandard;
  _agentic_analysis → emit Claims) to the actual pipeline.
- Verified: only 2 of the 5 "island" slices need backfill. The others
  (steps/guardrails) are prerequisites for Phase 5/12 (planner/validator),
  not disconnected code — their consumers don't exist yet by design.

Simplified promise tags to Carson model (dropped TASK-DONE):
- Normal single-story completion = end response, NO tag → loop continues.
- <promise>COMPLETE</promise> ONLY when all stories pass → loop stops.
- BLOCKED/DECIDE → stop.

Root cause of the prior run's island problem: the plan had no connection
criteria per task, so "done" meant "module exists + tests pass" not "module
is wired to its consumer." The prd.json acceptanceCriteria now require
connection (e.g. "calculate_max_units accepts DistrictDimensionalStandard").
This is the structural fix.

<promise>COMPLETE</promise>
